# Text Analysis/ Mining
# 1. Get text data
    # a. direct download exisiting data
    # b. API
    # c. scrape webpage
# 2. Tokenization
    # a. remove {punctuation, spaces (split() function), numbers, tags, CSS formatting}
    # b. doc -> sentences -> words
    # c. lowercase
    # d. remove stopwords (pip install nltk - import stopwords) (nlt.corpus)
    # e. lemmatize/stemming (using NLTK WordNetLemmatizer())
# 3. bag of words, phrases, common combinations
# 4. TF/IDF (Term Frquency/ Inverse Document Frequency)
# 3. Analysis
    # a. objectives